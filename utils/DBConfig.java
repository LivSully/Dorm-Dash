package utils;

// This is the configuration class for database connection values
public class DBConfig {

    // This is storing the database URL
    public static final String URL = "jdbc:mysql://localhost:3306/DormDash";

    // This is storing the database username
    public static final String USERNAME = "root";

    // This is storing the database password
    public static final String PASSWORD = "newpassword123";
}